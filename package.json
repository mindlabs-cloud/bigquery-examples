{
  "name": "stackoverflow-ai",
  "engines": {
    "node": "16.14.2",
    "npm": "8.5.0"
  },
  "scripts": {
    "prepare": "npx dataform install",
    "build": "npx dataform compile",
    "test": "npx dataform test"
  },
  "dependencies": {
    "@dataform/core": "2.0.1",
    "@dataform/cli": "^2.0.0"
  }
}